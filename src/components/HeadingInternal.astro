---
import { Image } from 'astro:assets';
import Navigation from './Navigation.astro';
import logoSm from '../assets/images/logo-small.png';

const {class: className, ...rest } = Astro.props;
---
<header class=`header ${className}` {...rest}>
  <div class="inner-layout">
    <h1 class="visually-hidden">Therapy & Consultation in Asheville, NC</h1>
    <div class="title"><a href="/" class="title-link"><span class="name">Teresa B. Fisher PhD.</span><Image src={logoSm} class="logo-small" alt="logo" width="100" /><span class="llc">Milkweed Mental Health</span></a></div>
    <Navigation />
  </div>
</header>

<style>
  .visually-hidden {
    position: absolute !important;
    height: 1px; width: 1px;
    overflow: hidden;
    clip: rect(1px, 1px, 1px, 1px);
    white-space: nowrap;
    border: 0;
    padding: 0;
    margin: -1px;
  }
  .title-link  {
    display: grid;
    grid-column-gap: 1rem;
    justify-self: center;
    align-items: center;
    text-decoration: none;
    color: var(--color-primary);
  }
  .title {
    font-size: 1.35rem;
    color: var(--color-primary);
    grid-template-columns: auto 1fr;
    grid-template-rows: auto auto;
    margin: .5rem 0;
    font-family: "Englebert", serif;
    letter-spacing: 2px;
    line-height: 1.25;
  }
  .title .name {
    grid-column-start: 2;
    grid-row-start: 1;
    align-self: end;
    justify-self: start;
  }
  .title .llc {
    grid-row-start: 2;
    grid-column-start: 2;
    align-self: start;
  }
  .title .logo-small {
    grid-column-start: 1;
    grid-row-start: 1;
    grid-row-end: 3;
    justify-self: center;
  }
  @media screen and (min-width: 650px) {
    .title .name,
    .title .llc {
      white-space: nowrap;
    }
    .title .name {
      grid-column-start: 1;
      grid-row-start: unset;
      align-self: center;
      justify-self: end;
    }
    .title .llc {
      grid-row-start: unset;
      grid-column-start: 3;
      align-self: center;
    }
    .title .logo-small {
      grid-column-start: 2;
      grid-row-start: unset;
      grid-row-end: unset;
    }
  }
</style>
---
import Images from './Images.astro';
const { title, url, alt, width, height, ...rest } = Astro.props;
---
<div class="cube" {...rest}>
  <span class="title">{title}</span>
  <Images class="image" src={url} alt={alt} width={width} height={height} />
</div>

<style>
	.cube {
		display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-end;
    text-align: center;
	}
	.title {
		font-size: 1rem;;
	}
</style>